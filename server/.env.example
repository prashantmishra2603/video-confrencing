# MongoDB Connection String (use MongoDB Atlas for production)
MONGODB_URI=mongodb+srv://pmishrajiup53_db_user:pm123456@cluster0.vg1k9zu.mongodb.net/videoDB?retryWrites=true&w=majority

# JWT Settings
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Server Port (Render will override this with process.env.PORT)
PORT=5000

# Environment (set to production on Render)
NODE_ENV=development

# Frontend URL (update this to your deployed frontend URL on Render)
CLIENT_URL=http://localhost:3000
